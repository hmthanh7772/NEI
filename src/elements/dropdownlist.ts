import { Element } from './element';

/**
 * Dropdown element, point to an 'li' element
 */
export class NEIDropdown extends Element {

    constructor(locator: string) {
        super(locator);
    }

}